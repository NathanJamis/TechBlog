<div class="comments card m-5">
    {{#each this}}
    <div class="card-body">
        <h5 class="card-title">{{user.username}} on {{timestamp}}</h5>
        <p class="card-text">{{comment_text}}</p>
    </div>
    {{/each}}
</div>